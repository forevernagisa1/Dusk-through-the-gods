For Windows：

1.在代码仓库中保存并下载Dusk-through-the-gods v1.0 文件夹

2.打开游戏根目录后将“Run in Windows.zip”解压到当前文件夹
——这一步是将游戏项目部署到Windows运行环境下

3.打开游戏根目录下的www文件夹，找到“Art Resources.zip”后解压到当前文件夹
——这一步是释放游戏的美术资源

4.返回游戏根目录后运行“Game.exe”即可进入游戏

For Android：

按照windows部署操作之后，使用APK打包文件将”www”文件夹打包，之后将生成的apk安装到对应环境即可进入游戏。